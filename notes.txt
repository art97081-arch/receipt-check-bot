Owner TG ID: set in Railway as OWNER_TG_ID.
DataGrab key: set in Railway as DATAGRAB_KEY.
Bot token: set in Railway as BOT_TOKEN.

Security: Do not commit .env with real values. Use .env and .gitignore to prevent commits.
